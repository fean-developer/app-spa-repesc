export class Clients {
    protected access_token!: string;
    protected token_type!: string;
    protected expires_in!: number;
    protected refresh_token!: string;
    protected ensurance_key?: string;
    protected encrypt_key!: string;
}

